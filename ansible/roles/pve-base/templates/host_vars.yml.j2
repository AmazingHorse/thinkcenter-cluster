# Auto-generated from cluster-manifest.yml via Ansible 00-generate-boot-config.yml
# DO NOT EDIT DIRECTLY — Edit cluster-manifest.yml at root instead

node_mac: "{{ item.mac }}"
node_ram_mb: {{ item.ram_mb }}

storage_profile: "{{ item.storage_profile }}"
os_disk: "{{ item.os_disk }}"
ceph_osd_disk: "{{ item.ceph_osd_disk }}"
ceph_osd_partition: "{{ item.ceph_osd_partition }}"

zfs_arc_max_mb: {{ item.zfs_arc_max_mb }}

cluster_links:
{{ item.links | to_nice_yaml(indent=2) }}

corosync_ring1_addr: "{{ item.corosync_ring1_addr }}"

udev_rules:
{{ item.udev_rules | to_nice_yaml(indent=2) }}
