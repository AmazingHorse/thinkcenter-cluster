{
  "id": "{{ item.name }}",
  "name": "{{ item.name }}",
  "profile": "pxe-proxmox-{{ item.name }}",
  "selector": {
    "mac": "{{ item.mac | lower }}"
  },
  "metadata": {
    "hostname": "{{ item.name }}"
  }
}
